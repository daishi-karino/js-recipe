! を入力して タブ でいい感じのフォーマット出るよ！

他の言語で、「値が等しいか」を判断するための比較演算子として、「==」 をみたことがある人がいるかもしれません。JavaScriptでも==を使うことはできます。  === は「データ型まで同じか」を判定してくれます。 プログラマーの意図しないミスを防ぐため、特に理由がなければ、=== を利用するといいと思います

オブジェクト: keyとvalueの組み合わせ。 あとでみんなに書いてもらうので、集中してみててください！

const Daishi = {
    name: "だいし",
    hobbies: ["だいし","だいし"]
    family: {
        papaname: "太郎",
        mamaname: "まま"
    }
}

console.log(Daishi.name)

document object model = DOM